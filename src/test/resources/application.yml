spring:
  cache:
    redis:
      time-to-live: 1D
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:jlog
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false
  output:
    ansi:
      enabled: always
