spring:
  output:
    ansi:
     enabled: always
  config:
    activate:
      on-profile: local
  datasource:
    username: root
    password: 15411541
    url: jdbc:mariadb://localhost:13306/jlog?serverTimezone=Asia/Seoul
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
